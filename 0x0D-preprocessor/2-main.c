#include <stdio.h>

/**
 * main - Prints the name of the file of the program
 * was complied from, followed by new line
 *
 * Return: Always 0
 */
int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
