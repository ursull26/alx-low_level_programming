#include "main.h"

/**
 * main - Prints alphabets
 *
 * Return: Always 0.
 */
int main(void)
{
	char alpha;
	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		_putchar(alpha);
	}
	_putchar('\n');

	return (0);
}
